# Student Registration System

The system allows registration of students along with their face images.

Each student can have multiple face images stored in the database.

This improves face recognition accuracy because the system can compare multiple facial variations.

Workflow:

Student registers
        ↓
Uploads face images
        ↓
Images stored in media/student_faces
        ↓
Database stores image paths